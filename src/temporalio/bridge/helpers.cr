module Temporalio
  module Bridge
    module Helpers
      # NOTE: This module is kept for backward compatibility.
      # The Rust extension (Ext) handles all data conversion internally.
      # These helper methods are no longer used in production code.
    end
  end
end
