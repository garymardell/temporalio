require "spec"

# Test if FFI callbacks from external threads work in Crystal

describe "External Thread Callbacks" do
  pending "can receive callback from pthread created thread - needs test C library"
end
